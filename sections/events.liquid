<div class="w-full h-auto p-8 flex flex-col gap-y-12">
  <div class="w-full h-auto flex justify-start items-start p-4">
    <h2 class="text-5xl">{{ section.settings.section_heading }}</h2>
  </div>

  {% for block in section.blocks %}
    {% if block.type == 'old_events' %}
      <div class="w-full h-auto relative bg-bg-secondary flex flex-col gap-y-8 p-8">
        <h2 class="absolute right-0 -top-4 text-3xl font-bold uppercase">Relive our Events</h2>
        <div class="flex items-start justify-start">
          <div class="flex flex-1 h-auto">
            <h2 class="text-2xl">{{ block.settings.event_title }}</h2>
          </div>
          <div class="w-auto h-auto">
            {% render 'arrow-up' %}
          </div>
        </div>
        <div class="flex items-start justify-start">
          <p class="text-base">{{ block.settings.event_description }}</p>
        </div>
        <div class="flex items-start justify-start">
         <div class="w-full h-500"></div>
        </div>
      </div>

    {% elsif block.type == 'oncoming_events' %}
      <div class="w-full h-auto relative bg-bg-secondary flex flex-col gap-y-8 p-8">
        <h2 class="absolute right-0 -top-4 text-3xl font-bold uppercase">Save the date</h2>
        <div class="flex flex-row gap-x-20 items-start justify-start">
          <div class="flex flex-row gap-x-6 items-center justify-center">
            {% render 'calendar' %}
            <p class="text-base font-bold">{{ block.settings.event_date }}</p>
          </div>
          <div class="flex flex-row gap-x-6 items-center justify-center">
            {% render 'earth' %}
            <p class="text-base font-bold">{{ block.settings.event_place }}</p>
          </div>
        </div>
        <div class="flex items-start justify-start">
          <p class="text-base">{{ block.settings.event_description }}</p>
        </div>
      </div>
    {% endif %}
  {% endfor %}
</div>

{% schema %}
  {
    "name": "events",
    "settings": [
      {
        "type": "text",
        "id": "section_heading",
        "label": "Titolo sezione",
        "default": "Events",
        "info": "Inserisci qui il titolo della sezione."
      }
    ],
    "blocks": [
      {
        "type": "old_events",
        "name": "Old Events",
        "settings": [
          {
            "type": "text",
            "id": "event_title",
            "label": "Titolo evento",
            "default": "Christmas Dinner 2023",
            "info": "Inserisci qui il titolo dell'evento."
          }, {
            "type": "text",
            "id": "event_date",
            "label": "Data evento",
            "default": "26/06/2024",
            "info": "Inserisci qui la data dell'evento."
          }, {
            "type": "text",
            "id": "event_place",
            "label": "Luogo evento",
            "default": "Subotica",
            "info": "Inserisci qui il luogo dell'evento."
          }, {
            "type": "textarea",
            "id": "event_description",
            "label": "Descrizione evento",
            "default": "Lorem Ipsum....",
            "info": "Inserisci qui la descrizione dell'evento."
          }
        ]
      }, {
        "type": "oncoming_events",
        "name": "Oncoming Events",
        "settings": [
          {
            "type": "text",
            "id": "event_date",
            "label": "Data evento",
            "default": "26/06/2024",
            "info": "Inserisci qui la data dell'evento."
          }, {
            "type": "text",
            "id": "event_place",
            "label": "Luogo evento",
            "default": "Subotica",
            "info": "Inserisci qui il luogo dell'evento."
          }, {
            "type": "textarea",
            "id": "event_description",
            "label": "Descrizione evento",
            "default": "Lorem Ipsum....",
            "info": "Inserisci qui la descrizione dell'evento."
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "events",
        "category": "Custom"
      }
    ]
  }
{% endschema %}